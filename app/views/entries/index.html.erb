<h1>To Do</h1>
<%= link_to "New Entry", "/entries/new" %>
<ul>
  <% @entries.each do |entry| %>
    <h1>Entry <%= entry.id %>:</h1>
    <h2><%= link_to entry.title, entry %><h2>
    <h3><%= entry.body %><h3>
    <h4><%= link_to "Edit", edit_entry_path(entry) %></h4>
  <% end %>
</ul>
